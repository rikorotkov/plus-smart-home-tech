server:
  port: 0

spring:
  datasource:
    driver-class-name: "org.postgresql.Driver"
    url: "jdbc:postgresql://localhost:6543/payment"
    username: "postgres"
    password: "postgres"
  sql:
    init:
      mode: always

tax: "0.1"