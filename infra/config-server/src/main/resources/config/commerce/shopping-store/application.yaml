server:
  port: 0

spring:
  datasource:
    driver-class-name: "org.postgresql.Driver"
    url: "jdbc:postgresql://localhost:6543/shopping_store"
    username: "postgres"
    password: "postgres"
  sql:
    init:
      mode: always